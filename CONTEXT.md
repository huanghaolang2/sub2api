# Sub2API 领域语言

该文件定义产品与代码讨论中必须统一使用的业务术语，避免把模型展示、可用性和计费配置混为一体。

## 模型与价格

**模型标识（Model Key）**：
请求、路由和定价匹配使用的稳定模型名称；同一展示模型可以对应多个模型标识。
_Avoid_: 展示名、模型标题

**模型展示资料（Model Profile）**：
面向用户展示的模型名称、说明、能力标签、排序和可见性等资料；默认不改变路由与计费。
_Avoid_: 模型定价、模型配置

**定价规则（Pricing Rule）**：
管理员为模型标识配置的计费模式、价格维度和可选阶梯区间。
_Avoid_: 展示价格、官方价格

**官方参考价（Official Reference Price）**：
来自外部价格目录的只读参考价格，不等同于用户最终支付价格。
_Avoid_: 售价、生效价格

**生效价格（Effective Price）**：
应用分组或渠道覆盖、用户专属倍率及适用时段倍率后，当前用户实际适用的展示与计费价格。
_Avoid_: 官方参考价、基础价格

**模型可用性（Model Availability）**：
当前身份能够实际选择和调用某模型标识的资格；展示可见不代表一定可调用。
_Avoid_: 模型展示、模型状态

## 使用记录与看板

**API Key 展示名称（API Key Display Name）**：
使用看板中用于标识用量归属的密钥名称；不同密钥可以同名，但仍分别统计。图表系列与结果表均围绕 API Key 展示名称和用量呈现。
_Avoid_: 负责人、平台负责人
